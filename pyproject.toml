[project]
name = "notemind"
version = "0.1.0"
description = "NoteMind is a tool that turns your notes into a searchable knowledge base using retrieval-augmented generation (RAG)."
authors = [
    {name = "madargani",email = "mad3913@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.group.dev.dependencies]

